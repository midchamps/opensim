/**
 * Standard 3D environments (Three.js + react-three-fiber).
 *
 * `BaseLabScene` (Phase 1.3) — desk + lighting + OrbitControls camera.
 * `_TemplateLabScene` (later) — copy-and-customize starter for archetypes
 *   that need a specialized environment (e.g. `agent_based` wants
 *   PetriDish on the desk by default).
 */
export {};
