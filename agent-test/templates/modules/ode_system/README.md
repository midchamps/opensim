# `ode_system` Archetype Template

Phase-3 placeholder. The real solver / lab_object content for ODE
simulators (RK4, RK45, BaseODE, _TemplateODE, Pendulum, Spring,
TimeSeriesPlot, PhasePortrait) lands in **Phase 4** — see
`.harness/plans/active/20260426-opensim-bootstrap.md`.

For now this directory exists only so `classify_simulation_type`'s
scaffolding instructions (`cp -r .../modules/ode_system/src/* ./src/`)
do not error. The `src/` subtree contains a single placeholder file
that documents the next phase's deliverables.
