# `ode_system` Documentation

Phase-3 placeholder. Real `design_rules.md` and `template_api.md` for
the ODE archetype land in **Phase 4**. Until then `generate_protocol`
falls back to its built-in archetype stub and produces a usable
Protocol Document anyway — the agent simply gets less archetype-
specific guidance.
