import Phaser from 'phaser';
import { BaseObstacle } from './BaseObstacle';
export declare class _TemplateObstacle extends BaseObstacle {
    constructor(scene: Phaser.Scene, x: number, y: number);
}
